---
name: feishu-cloud-logs
description: "基于飞书 Docx 或 Wiki-backed Docx 创建、审阅、追加、插入、回填、更正、重组和归档发布、工程、项目、事故、决策、实验、审计或客户支持等专业叙事日志。用于要求可验证事实、时间语义和历史保全的日志记录；普通文档或手册、行式数据或统计、任务分配、会议原始产物、云盘文件管理以及服务器运行日志不使用本技能。"
---

# 飞书云日志

## 定位

- 把技术与业务变化整理成专业、可验证、可追溯的飞书云文档日志。
- 处理发布／产品变更、工程变更、项目／进展、事故／运维、决策、实验／研究、审计／合规、客户反馈／支持八类日志。
- 支持只生成草稿，也支持在用户目标明确时安全维护既有日志。
- 编辑既有日志时保留实时文档的结构和风格；实时状态始终覆盖过时记忆。

## 先做路由

- 专业叙事日志写入 Docx 或 Wiki-backed Docx 时，使用本技能。
- 普通方案、手册或任意段落编辑，路由到 `lark-doc`。
- 解析 Wiki 节点或决定知识库放置位置，路由到 `lark-wiki`。
- 行、筛选、聚合或统计记录，路由到 Base 或 Sheets。
- 分配负责人、截止日期或提醒，路由到 Task。
- 整理原始会议纪要、逐字稿或录音产物，路由到 Minutes。
- 上传、下载、移动、复制文件或文件夹，以及权限和评论等云盘文件管理，路由到 `lark-drive`。
- 服务器、应用或基础设施的运行日志流不在本技能范围内。
- 允许组合路由：其他能力收集事实，本技能负责叙事日志与历史语义。

## 加载运行能力

1. 读取当前运行时版本匹配的 `lark-shared`、`lark-doc` 和 `lark-wiki` Skill。
2. 按这些 Skill 的要求加载其当前参考文档，不复制易过期的命令说明。
3. 使用用户身份作为默认身份；不得自动切换为机器人。
4. 权限不足时只请求完成当前动作所需的最小权限。
5. 运行时不可用时遵循能力检测和固定回退规则，不自行安装或认证。

## 执行工作流

1. 识别日志类型、受众、敏感级别、生命周期与行动要求。
2. 明确发生、生效或发布、记录、更新四类时间语义；未知值标记待确认。
3. 优先使用用户明确给出的 URL 或标识，解析实时规范目标。
4. 读取完成任务所需的最小实时范围，并确认文档类型、结构和写入位置。
5. 仅在实时 URL 与目标 token 精确匹配时应用私有文档档案；Wiki 目标还必须精确核对实时解析出的底层 Docx token；别名只产生候选，不授予写入权限。
6. 根据受众把技术变化翻译为可理解的影响，同时保留风险、限制和验证依据。
7. 区分事实、推测、决定和待确认事项，不补造时间、指标、负责人或根因。
8. 使用 `scripts/lint_log_draft.py` 离线检查字段、隐私、层级与视觉回退。
9. 用户只要求草稿时停止在草稿，不执行任何云端写入。
10. 用户本轮已明确要求写入，且私有解析器对精确目标（Wiki 同时包含节点与底层 Docx）、`user` 身份、日志类型、受众和追加操作返回 `may_mutate=true`、`trust_status=trusted` 时，直接执行普通追加，不重复询问；信任标识不自行产生写入意图。
11. 写入时使用当前 revision 做乐观并发控制，并为创建或追加设置稳定锚点。
12. 写后重新读取目标位置，核对结构、正文、顺序与历史完整性。

## 内容与历史规则

- 不得用提交日期冒充发布日期；回填必须标明原事件和回填关系。
- 预告必须明确为未生效内容，不得写成已经发布或已经交付。
- 事故、审计和决策日志采用追加式更正，禁止静默改写已发布历史。
- 面向客户时先写影响和使用变化；面向工程团队时补充验证、风险和回滚条件。
- 面向管理层时突出状态、取舍和下一里程碑；面向审计人员时突出范围和证据链。
- 新建文档采用连续标题、短段落、短列表和带文字的状态标签。
- 既有文档的稳定标题、排序、保护区域和视觉风格优先，不强行重构。

## 写入安全

- 密钥、Cookie、凭据和认证材料禁止进入日志，即使用户要求保留。
- 个人信息默认最小化或脱敏；上传媒体前检查客户信息、邮箱、电话和元数据。
- 写前检查同名文档；父级标识和父级位置参数只使用一种。
- 仅在精确块操作时获取块 ID；块 ID 只在本次操作内使用，不写入档案。
- revision 冲突后重新读取、重新定位并重新生成补丁，不覆盖并发修改。
- 超时、缺少返回、warning、`partial_success` 或媒体状态不明都视为结果未知。
- 结果未知时先按目标、稳定标题、逻辑锚点和预期内容读回，不盲目重试。
- 创建和追加不得直接重复执行；先排除已成功但响应丢失的情况。
- 缺少信任标识、标识无效／过期／已撤销、目标或范围不匹配时，不得把历史授权扩展到本次写入。
- 删除、覆盖、批量重排或历史恢复前，展示精确目标、影响范围和差异预览并取得确认。
- 只有用户明确要求“记住”“更新文档档案”或“信任／撤销信任该文档”时，才允许修改 `.local/`。
- 日志写入不得顺便修改 Skill、公共参考或私有档案。

## 成功判定

仅当以下条件全部满足时声明成功：

- 进程退出成功；
- 返回 `ok=true`；
- 业务结果为 `success`；
- warning 为空；
- 目标文档和写入位置正确；
- 写后读回的正文与预期等价。

任一条件缺失时报告“结果未知”或明确失败，并给出已核验事实与安全下一步。

## 按需读取参考

- 当需要选型、字段或时间语义时，读取 [日志分类](references/log-taxonomy.md)。
- 当需要起草八类日志时，读取 [日志模板](references/templates.md)。
- 当需要适配受众、证据与不确定性表达时，读取 [写作质量](references/writing-quality.md)。
- 当需要表格、图表、图片或白板时，读取 [版式与视觉](references/layout-and-visuals.md)。
- 当需要匹配、迁移或维护私有档案时，读取 [私有记忆合同](references/private-memory-contract.md)。
- 当需要运行时选择、写入、重试或破坏性操作时，读取 [执行与安全](references/execution-and-safety.md)。
