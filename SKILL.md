---
name: feishu-cloud-logs
description: "起草、审阅和维护飞书 Docx / Wiki-backed Docx 的发布、工程、项目等叙事日志，保留可验证事实、时间语义与历史。普通文档、行式数据和服务器运行日志不适用。"
---

# 飞书云日志

## 定位

把技术与业务变化整理成受众能理解的日志，支持发布／产品变更、工程、项目／进展、事故／运维、决策、实验／研究、审计／合规和客户反馈／支持八类记录。可起草、审阅、创建、追加、插入、回填、更正、重组或归档；编辑时沿用实时文档的结构和风格。

## 先做路由

- 普通方案、手册或任意段落编辑交给 `lark-doc`；Wiki 节点和知识库放置交给 `lark-wiki`。
- 行式数据与统计使用 Base 或 Sheets；任务分配与提醒使用 Task；原始会议纪要、逐字稿或录音产物使用 Minutes。
- 云盘文件管理、权限和评论交给 `lark-drive`；服务器或应用运行日志流不在本技能范围内。
- 其他能力可以收集事实，本技能只承担叙事日志与历史语义。

## 按任务加载能力

- 只依据已提供材料起草或审阅时，不加载飞书技能、不检查认证、不探测运行时。若用户要求参考云端内容，只加载所需读取能力；草稿请求不包含写入授权。
- Docx 读取、创建或更新只加载 `lark-doc` 及当前操作参考；需要 Wiki 解析时再加载 `lark-wiki`。认证依赖遵循对应技能的当前规则，不额外统一预读 `lark-shared`。
- 同一会话已读且仍适用的技能、参考和能力检测结果直接复用。按 `compatibility.json` 的操作 profile 选择能力，不把完整诊断集合当作每次的前置条件。
- 默认使用用户身份，不自动切换机器人；权限不足时只处理当前动作所缺权限。运行时不可用时按执行参考处理固定回退，不自行安装或认证。

## 执行工作流

1. 识别用户要草稿、审阅还是云端维护，以及日志类型、受众和真实状态。只保留当前记录需要的信息，不为无关字段补占位内容。
2. 需要云端读取或写入时，优先解析用户明确给出的 URL 或标识，读取目标与相邻章节的最小范围，确认结构和位置。只有实时规范目标精确匹配才应用私有档案；Wiki 同时核对节点与底层 Docx。别名只产生候选，没有档案也可依据实时文档继续。
3. 起草影响、必要限制、行动要求与可核验依据。区分事实、推测、决定和待确认的语义，不要求四个独立栏目；不编造时间、指标、负责人或根因。
4. 有新写或改写正文时，用 `scripts/lint_log_draft.py --log-type <类型> --profile <full|append> --file <草稿路径>` 离线检查。独立完整记录用 `full`；沿用已读取或用户提供的既有章节上下文的片段用 `append`，不重复完整字段与日期时区。省略 profile 仍默认 `full`。纯删除或重排没有新增正文时检查差异与范围，不生成空草稿运行 lint。只要求草稿时交付草稿并停止。
5. 普通追加已有会话内明确授权且目标、身份和范围未变时直接执行，不要求先建立私有信任记录。持久信任路径由 `private_memory.py resolve` 判定；其 `may_mutate` 只描述该路径，不是统一写入门槛。信任记录不产生写入意图。
6. 写前核对当前 revision、稳定锚点、保护区域与重复内容；写后读回，核对正文、位置、顺序和历史完整性。

## 内容与历史规则

- 不得用提交日期冒充发布日期；回填必须标明原事件和回填关系。
- 预告必须明确为未生效内容，不得写成已经发布或已经交付。
- 事故、审计和决策日志采用追加式更正，禁止静默改写已发布历史。
- 面向客户或运营人员先写影响和使用变化；工程团队需要验证、风险与回滚条件；管理层需要状态和取舍；审计人员需要范围与证据链。
- 新建文档采用连续标题、短段落、短列表和带文字的状态标签；简短追加可沿用已确认的章节日期与时区。
- 既有文档的稳定标题、排序、保护区域和视觉风格优先，不强行重构。

## 写入安全

- 密钥、Cookie、凭据和认证材料禁止进入日志，即使用户要求保留。
- 个人信息默认最小化或脱敏；上传媒体前检查客户信息、邮箱、电话和元数据。
- 创建前检查同名文档；父级标识和父级位置参数只使用一种。
- 仅在精确块操作时获取块 ID；块 ID 只在本次操作内使用，不写入档案。
- revision 冲突后重新读取、重新定位并重新生成补丁，不覆盖并发修改。
- 超时、缺少返回、warning、`partial_success` 或媒体状态不明都视为结果未知。
- 结果未知时先按目标、稳定标题、逻辑锚点和预期内容读回，不盲目重试。
- 创建和追加不得直接重复执行；先排除已成功但响应丢失的情况。
- 缺失、无效、过期或撤销的信任记录不能支持历史授权复用；依据当前会话判断明确授权，不自动补写 grant。目标不明确或已授权范围发生变化时，只澄清缺失或变化部分。
- 删除、覆盖、批量重排或历史恢复前，完成精确目标、影响范围、差异预览及恢复说明；已有授权覆盖该具体预览时不重复确认，否则取得确认后再执行。
- 只有用户明确要求“记住”“更新文档档案”或“信任／撤销信任该文档”时，才允许修改 `.local/`。
- 日志写入不得顺便修改 Skill、公共参考或私有档案。

## 成功判定

云端写入仅在进程退出成功、返回 `ok=true`、业务结果为 `success`、warning 为空、目标和位置正确，且写后读回正文与预期等价时声明成功。

任一条件缺失时报告“结果未知”或明确失败，并给出已核验事实与安全下一步。

## 按需读取参考

- 当需要选型、字段或时间语义时，读取 [日志分类](references/log-taxonomy.md)。
- 当需要完整记录模板或简短追加示例时，读取 [日志模板](references/templates.md)。
- 当需要适配受众、证据与不确定性表达时，读取 [写作质量](references/writing-quality.md)。
- 当需要表格、图表、图片或白板时，读取 [版式与视觉](references/layout-and-visuals.md)。
- 当需要匹配、迁移或维护私有档案时，读取 [私有记忆合同](references/private-memory-contract.md)。
- 当需要运行时选择、写入、重试或破坏性操作时，读取 [执行与安全](references/execution-and-safety.md)。
